import { Card } from '@mui/material';
import styled from 'styled-components';

export const StyledDetailsView = styled(Card)`
  width: 100%;
  padding: 2rem 1rem;
`;

export const ActionsRow = styled.div`
  display: flex;
  gap: 1rem;
  margin-top: 2rem;
  flex-wrap: wrap;
`;
